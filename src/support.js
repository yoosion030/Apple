const questionBox = document.querySelector('.questionBox');
const accoridion = document.querySelector('.accordion');
const img = document.querySelector('img');

questionBox.addEventListener('click',()=>{
    accoridion.
})